# 快速部署

這份文件只給「照著做就能把系統跑起來」的最短路徑，架構、模組職責等說明見 `README.md`；門禁子系統技術細節見 `architecture.md`，RAG 子系統見 `RAG.md`。

## 環境需求

- Windows（`vision_core.py` 目前依賴 Windows 內建字型 `msjh.ttc` 做中文渲染，跨平台需自行更換字體路徑）
- Python 3.10.x（主環境）＋ Python 3.11 以上（僅 LiteLLM Proxy 需要，見下方步驟 3）
- 若要用 GPU 跑人臉辨識／活體偵測：NVIDIA 顯卡 + 對應版本 CUDA Toolkit + cuDNN

## 步驟 1：建立主環境

```powershell
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
pip install retinaface --no-deps
```

`retinaface` 一定要分成兩個指令、順序不能反：這個套件在 PyPI 上宣告依賴 `tensorflow==2.5.0`，如果跟其他套件一起裝，會把 `requirements.txt` 裝好的 `tensorflow==2.21.0` 覆蓋掉，所以刻意不寫進 `requirements.txt`，裝完其他套件後再用 `--no-deps` 單獨補裝。

有 NVIDIA 顯卡的話，可以把 `requirements.txt` 裡的 `onnxruntime==1.23.2` 改成 `onnxruntime-gpu==1.23.2`（兩者互斥，不能同時裝）。

## 步驟 2：補齊模型檔案

`models/` 資料夾內的大型模型檔案（`arcface_r100_v1.onnx`、`yolov8s.pt`、`MiniFASNetV2.onnx`、TTS 語音模型等，約 490MB）沒有隨版本控制提供，需要另外取得後放進 `models/` 資料夾。

## 步驟 3：建立 LiteLLM Proxy 專用環境（僅需一次）

RAG 子系統的自動化評估功能（`evaluate_rag.py`）透過 LiteLLM Proxy 呼叫外部 API LLM 當裁判官，而 `litellm[proxy]` 目前所有相容 `openai` 2.x 的版本都用到 `NotRequired` 這個 Python 3.11 才有的語法，在 3.10 環境下會直接 `ImportError`，所以這一步需要另開一個獨立的 Python 3.11+ 虛擬環境：

```powershell
py -3.11 -m venv venv_litellm
venv_litellm\Scripts\activate
pip install "litellm[proxy]"
```

只需要建立一次，之後啟動系統不用重複執行。

## 步驟 4：啟動管理後台

回到主環境（`venv`，Python 3.10）：

```powershell
venv\Scripts\activate
python backend_main.py
```

管理後台畫面上方有三個各自獨立、互不影響的服務開關，啟動前會先偵測對應的網路埠是否已被占用：

- **啟動 RAG（Ollama + LiteLLM + 助理網頁）**：依序啟動 `ollama serve`、`litellm --config config.yaml`（使用 `venv_litellm`）、`app.py`，啟動後於瀏覽器開啟 `http://127.0.0.1:7860`。
- **啟動人臉辨識終端機**：啟動 `terminal_app.py`，另開攝影機畫面視窗。
- **啟動 QR 註冊伺服器**：啟動 `web_server.py`，供「遠端 QR Code 註冊」功能使用。

三個開關互相獨立，只需要哪個功能就開哪個。

## 相關但尚未整合的元件：WiFiDirectHotspotCore

`~/Downloads/WiFiDirectHotspotCore/`（跟本專案分開的獨立資料夾）是一支用 C++/WinRT 寫的 Windows 主控台程式，用來建立 WiFi Direct 熱點，設計上未來可讓手機在沒有既有 WiFi 環境時，直接連到執行門禁系統的電腦做 QR Code 註冊。目前已經做好「可被外部程式透過具名 Windows 事件觸發關閉」的機制，但 `backend_main.py` 尚未實際呼叫它，兩邊還沒串接，這裡先記錄其存在與規劃用途，之後串接完成再補進正式的部署流程。

## 常見問題

- **SQLite `database is locked`**：高併發寫入時可能出現，屬已知限制。
- **`RetinaFace` 在極低光環境下偵測率下降**：目前沒有自動補光邏輯。
- 其餘已知限制與技術決策原因見 `README.md`「已知限制」章節、`architecture.md`、`RAG.md`。
