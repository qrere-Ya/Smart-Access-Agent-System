# 尚未開始項目：最快能完成的排序，以及這次已直接動手完成的部分

你要的「尚未開始」項目分析，按實際能多快做完＋風險高低排序。今天已經直接動手做完最快的 4 項（詳見下方「✅ 已完成」），其餘按等級列出，供你決定下一步要不要繼續。

## ✅ 已完成（這次直接動手，零設計決策、風險最低）

這 4 項全部屬於健檢報告 P3「品質細節」跟學術強化「方向 5」，本來就是程式碼本身有明確答案的機械式修正，不需要你先做任何取捨，所以直接做完了：

1. **`config.yaml` 精簡重複的 LiteLLM 模型別名**
   拿掉 `claude-opus-5`、`claude-opus-4-8`、`claude-sonnet-4-6`、`claude-3-5-sonnet-20241022`、`claude-haiku-4-5-20251001` 這 5 組跟萬用規則 `claude-*` 內容完全重複的設定，只留 `claude-*` 跟 `gpt-4o` 兩組。`evaluate_rag.py` 現在預設用 `gpt-4o` 當出題官別名，不會再用到那 5 組舊別名，拿掉不影響任何現有呼叫。

2. **`security.py` 註解方向＋命名錯字修正**
   第 29 行註解原本寫「小於」，但下面判斷式其實是「大於」（`box_area > frame_area * 0.10`）——已改成跟程式碼一致；`copped_body`／`copped_head`／`boby_resized` 這三個打字錯誤的變數名，改成正確的 `cropped_body`／`cropped_head`／`body_resized`（純粹重新命名，不影響任何邏輯）。

3. **`vision_core.py` 移除 bare except／修好殭屍測試碼**
   `check_liveness()` 原本 `except:` 把所有例外都吃掉，改成 `except Exception as e:` 並印出實際錯誤內容，以後除錯不會兩眼一抹黑；行為不變，一樣回傳 `-1.0`。`__main__` 測試區塊原本直接把檔案路徑字串傳給只吃圖片陣列的 `face_detect_bgr()`，一執行就報錯——已經補上 `cv2.imread()` 把路徑讀成真正的圖片陣列，讓這段測試重新變成一段跑得動的自我測試。

4. **學術強化方向 5・擴大評估樣本數（3 → 20 題）**
   `evaluate_rag.py` 的 `run_dynamic_evaluation()` 預設題數從 `questions_per_category=3` 改成 `20`。另外發現一個沒人提過的小陷阱：「正負例」這一類是從固定的 `GOOD_SAMPLE_QUESTIONS`／`BAD_SAMPLE_QUESTIONS` 題庫裡 `[:n]` 取題，原本題庫各只有 5 題——如果只改參數不擴充題庫，這一類會悄悄被截斷成只有 5 題，跟法規／考勤兩類的 20 題對不齊，統計會失真。已經把兩個題庫各自擴充到 20 題（涵蓋範圍對應 `check_semantic_guardrail()` 裡定義的合法／不合法範圍）。

以上 4 個檔案都已經用你電腦上目前的修改時間當版本鎖，寫回你的 `Smart_Access_Agent_System` 資料夾。因為這台沙盒沒有真的 Ollama／LiteLLM Proxy 可以實際跑起來，**麻煩你找時間在自己電腦上實際「Execute Evaluate」跑一次**，確認：
- 三類測驗都正確跑了 20 題（不再是 3 題）；
- 人臉辨識、尾隨偵測（`security.py`、`vision_core.py`）功能正常，沒有因為重新命名或修改例外處理而壞掉；
- LiteLLM Proxy（`litellm --config config.yaml`）能正常啟動、出題官/裁判官還是能正常呼叫。

跑完 20 題一輪的評估會比 3 題花更多時間（LLM 出題＋作答＋裁判都要跑 20 次），這點先跟你說一聲，如果覺得 20 題太久，也可以自己把 `questions_per_category` 改成你要的數字（例如 10）。

## 🟡 中等：程式碼本身不難，但需要你先做一個決定，或需要在真實硬體上驗證才算數

這幾項我沒有直接動手，因為要嘛牽涉到會影響「正在跑的打卡/考勤流程」這種風險稍高的地方，要嘛需要你先決定要什麼行為，不是單純的機械式修正：

- **`database_mgr.py` 硬編字串集中設定／連線池**：健檢報告點出的問題是「每個函式自己開關資料庫連線，沒有共用」，還有像 `07:40:00`、`12:00:00`、`16:30:00` 這種上下班時間判定字串直接寫死在 `log_attendance()` 裡。改連線池、把時間規則抽成設定，牽動的是「打卡當下狀態怎麼判定」這條核心邏輯，改壞了會直接影響考勤紀錄正確性，我想先問過你要不要做、要做到什麼程度，再動手比較保險。
- **WiFi 熱點三項小型設定化**（SSID／密碼可設定化、遠端關閉機制、訂閱 StatusChanged 事件二次確認）：程式碼本身不複雜，但這是另一個資料夾（`WiFiDirectHotspotCore`，WinRT C++），我這邊沒有 Visual Studio 可以編譯驗證，改完你還是要自己編譯、在真實網卡上測過才算數，所以不算「最快」。

## 🔴 較大：需要新設計、大量資料，或長時間才能跑完

這些不建議列進「最快」清單，因為都牽涉到要嘛設計新邏輯、要嘛蒐集/訓練資料，動輒數小時到數天：

- 混合型問題聯合查詢（同時查考勤＋法規）——需要新的路由/合併查詢邏輯設計。
- 「AI 代理」按鈕語音個人化歡迎詞、打卡時間窗個人化——需要串接歷史資料＋你決定歡迎詞內容／個人化規則。
- 學術強化方向 1～4（RAG 消融實驗、邊緣運算效能實測、活體偵測準確率、人臉辨識 FAR/FRR）——方向 3 還被「自訓練活體偵測模型」卡住（還沒真的蒐集/訓練資料），方向 2 是公認工作量最大的一項。
- WiFi 熱點的其餘項目（跟 backend_main.py 串接、IP 偵測共用模組）——牽涉兩個專案間的介面設計。

## 下一步

進度看板我還沒更新這 4 項（想等你實際跑過一次「Execute Evaluate」、確認功能沒壞掉再標成完成，跟先前「等你確認」的做法一致）。如果你這幾天會找時間測，我可以先幫你把這 4 項標成「已完成（沙盒已改好，等你在真實環境確認）」；如果你想先看下一批要做什麼，「🟡 中等」那兩類（`database_mgr.py`、WiFi 熱點三項）是接下來風險可控、可以繼續往下做的候選，其中 `database_mgr.py` 影響比較大，建議你先看過我上面列的具體改動範圍再決定要不要做。
